/** @type {import('next').NextConfig} */
const nextConfig = {
  output:'export',
}

module.exports = 

module.exports = {
    reactStrictMode: true, 
    images: {
      domains: ['panel.rahvin.ir'],
    },
  }